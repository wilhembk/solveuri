name: Codeberg Sync
on:
  push:
    branches:
      - '**'
  schedule:
    - cron: "0 */6 * * *"

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout"
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: mirror-repository
        uses: yesolutions/mirror-action@v0.7.0
        with:
          REMOTE: "https://codeberg.org/wilhembk/solveuri.git"
          GIT_USERNAME: wilhembk
          GIT_PASSWORD: ${{ secrets.CODEBERG_TOKEN }}
